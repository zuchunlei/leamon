具体的IO读写由操作系统来完成，将完成状态通知给应用。（应用注册完成状态的事件处理器），真正的异步。

其中具体的IO指的是，操作系统内核态的数据（Socket网络连接中Recv-Q中的数据）到上层应用中进程空间中的数据缓冲区（NIO中的ByteBuffer等）之间的拷贝。
IO操作：内核态到用户态的数据交换过程。

在java的编程中，NIO关注的是IO的就绪事件，而AIO关注的是IO的完成事件。